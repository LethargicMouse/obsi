A word that ends on `-j` is a property of an [[object]].
A word that ends on `-r` is a property of another property - a higher property.
Properties can be **appended** with an arbitrary number of higher properties.
Some properties are [[transitive property|transitive]].
